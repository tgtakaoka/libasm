name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: test
      run: cd tests; make test
    - name: make cli
      run: cd cli; make
